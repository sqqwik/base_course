import matplotlib.pyplot as plt
import numpy as np
a = 10
b = 6
N = 100

def ellipse(a, b, N):
    angle = np.linspace(0, 2*np.pi, N)
    x = a* np.cos(angle)
    y = b * np.sin(angle)
    ply.plot(x, y)
    plt.axis("equal")
    plt(show)
ellipse (a, b, N) 
plt.savefig("fig_7.png")